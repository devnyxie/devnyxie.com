# Pluto Template

A minimalist, fast, and accessible blog template built with `Next.js`, `Tailwind CSS`, and `MDX`.

> [!info]
> Originally, I've started creating this template using Nuxt.js for the sake of learning, but after few months I decided that `Vue`

## Custom Markdown Parser

Under the hood we are utilizing:

- `remark` for parsing markdown
- `rehype` for transforming HTML
- `shiki` for syntax highlighting
- `gray-matter` for parsing front matter
- `zod` for static content validation and typization
